saros::render_full_reports(files = Sys.getenv("QUARTO_PROJECT_OUTPUT_FILES"))

### IF THE ABOVE FAILS; RUN THE FOLLOWING INTERACTIVELY
# saros::render_full_reports(path = paths$site_drafts_completed)
